<header>
  <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark px-4">
    <div class="container-fluid">
     <a href="/" class="text-xl font-semibold text-gray-100 hover:text-gray-500 transition duration-300 no-underline font-sans">Scheduling System</a>
      {{#if user}}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
          aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav ms-auto mb-2 mb-md-0">
            {{#if user.isAdmin}}
              <li class="nav-item">
                <a href="/admin" class="nav-link">Admin Interface</a>
              </li>
            {{/if}}
            <li class="nav-item d-inline-flex align-items-center">
              <span class="text-white nav-link">Hi, {{ user.name }}</span>
            </li>
              <li class="nav-item">
                <a href="/users/{{ user.id }}" class="nav-link">Profile</a>
              </li>
            <li class="nav-item">
              <form action="/logout" method="POST" class="nav-item">
              <button type="submit" class="btn btn-outline-success my-sm-0 my-2">Logout</button>
              </form>
            </li>
          </ul>
        </div>
      {{/if}}
    </div>
  </nav>
</header>